import type {
  SearchGroupListType,
  SearchMainType
} from '~/types/search';

export const DEFAULT_SEARCH_MAIN_TYPE: SearchMainType = {
  searchQuery: '',
  reSearchQuery: []
}

export const DEFAULT_GROUP_LIST: SearchGroupListType = {
  groupList: []
};